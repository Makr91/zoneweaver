# Hyperweaver UI build artifact (fetched into ./ui by CI / locally; never formatted here)
ui/

# Generated by release-please on every release — its output style is not
# Prettier's; formatting it just fights the generator.
CHANGELOG.md

# Generated API docs (created by scripts/generate-docs.js).
# openapi.json carries Jekyll front matter so it is NOT valid JSON — Prettier's
# JSON parser chokes on the leading `---`. These are regenerated every build/CI,
# so they should never be formatted or hand-edited.
docs/api/openapi.json
docs/api/swagger-ui.html
docs/api/reference.md
